{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/L10-03 Push Express site to Docker Hub/package.json",
                "localRoot": "${workspaceFolder}/L10-03 Push Express site to Docker Hub"
            }
        }
    ]
}